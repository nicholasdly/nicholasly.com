import Title from "@/components/title";
import { formatDateString } from "../../page";

export const metadata = {
  title: "Staying open minded as a developer",
  publishDate: "2024-03-15",
};

<Title
  title="Staying open minded as a developer"
  date={formatDateString(metadata.publishDate)}
/>

There exists two types of developers. On one hand, we have the developer that is open to learning
new tools, frameworks, languages, and
